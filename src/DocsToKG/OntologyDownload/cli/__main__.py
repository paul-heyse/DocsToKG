# === NAVMAP v1 ===
# {
#   "module": "DocsToKG.OntologyDownload.cli.__main__",
#   "purpose": "Entry point for CLI invocation via python -m.",
#   "sections": []
# }
# === /NAVMAP ===

"""Entry point for CLI invocation via python -m."""

from DocsToKG.OntologyDownload.cli_main import app

if __name__ == "__main__":
    app()
